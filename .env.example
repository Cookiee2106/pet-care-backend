# ===========================================
# PET CARE BACKEND - ENVIRONMENT VARIABLES
# ===========================================
# Copy file này thành .env để chạy local
# Hoặc set các biến này trên Render khi deploy production
# ===========================================

# ==================
# SERVER CONFIG
# ==================
# Port mà server sẽ chạy (Render tự động set PORT, local dùng 9193)
PORT=9193

# ==================
# DATABASE CONFIG (AIVEN MYSQL)
# ==================
# Lấy thông tin này từ Aiven Console → Service → Overview
# Format: jdbc:mysql://HOST:PORT/DATABASE_NAME?ssl-mode=REQUIRED

# PRODUCTION (Aiven)
DB_URL=jdbc:mysql://your-aiven-host.aivencloud.com:port/pet_care_db?ssl-mode=REQUIRED
DB_USERNAME=avnadmin
DB_PASSWORD=your-aiven-password-here

# LOCAL (Nếu chạy MySQL local)
# DB_URL=jdbc:mysql://localhost:3306/pet_care_db
# DB_USERNAME=root
# DB_PASSWORD=admin

# ==================
# HIBERNATE CONFIG
# ==================
# Hiển thị SQL queries trong logs (set false trong production)
SHOW_SQL=false

# Log level cho SQL (DEBUG, INFO, WARN, ERROR)
LOG_SQL=INFO

# Hibernate DDL mode:
# - create: Tạo mới database mỗi lần start (XÓA DATA CŨ)
# - update: Cập nhật schema, giữ data cũ (KHUYẾN NGHỊ cho production)
# - create-drop: Tạo mới khi start, xóa khi stop
# - validate: Chỉ kiểm tra schema, không thay đổi
DDL_AUTO=update

# ==================
# JWT (Authentication)
# ==================
# JWT Secret key - PHỞ TẠO MỚI CHO PRODUCTION (tối thiểu 32 ký tự)
# Có thể generate tại: https://generate-random.org/api-key-generator
JWT_SECRET=36763979244226452948404D635166546A576D5A7134743777217A25432A462D

# JWT Token expiration time (milliseconds)
# 3600000 = 1 giờ
# 86400000 = 24 giờ
# 604800000 = 7 ngày
JWT_EXPIRATION=3600000

# ==================
# FRONTEND URL (CORS)
# ==================
# URL của frontend để cấu hình CORS
# LOCAL:
FRONTEND_URL=http://localhost:5174

# PRODUCTION (Render):
# FRONTEND_URL=https://pet-care-frontend.onrender.com

# ==================
# EMAIL CONFIG (Optional - nếu có email service)
# ==================
# Nếu muốn gửi email verification, cần cấu hình SMTP
# MAIL_HOST=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USERNAME=your-email@gmail.com
# MAIL_PASSWORD=your-app-password
# MAIL_PROTOCOL=smtp
# MAIL_SMTP_AUTH=true
# MAIL_SMTP_STARTTLS_ENABLE=true

# ==================
# NOTES
# ==================
# - KHÔNG commit file .env lên Git (đã có trong .gitignore)
# - Khi deploy lên Render: Set từng biến trong Environment tab
# - JWT_SECRET phải đổi mới cho production (security)
# - DDL_AUTO nên dùng 'update' trong production để không mất data
# - SHOW_SQL nên set false trong production để giảm logs
